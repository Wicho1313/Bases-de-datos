SQL> REM Union interna
SQL> SELECT course_no, s.section_no, c.description,
  2  	s.location, s.instructor_id
  3  	FROM course c INNER JOIN section s
  4  	USING (course_no);

no rows selected

SQL> spool off
