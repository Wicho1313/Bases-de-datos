SQL> PROMPT PASO 4
PASO 4
SQL> 
SQL> ALTER TABLE PAIS
  2  DROP CONSTRAINT PAIS_CONTINENTE_FK;

Table altered.

SQL> ALTER TABLE PAIS
  2  ADD CONSTRAINT PAIS_CONTINENTE_KF
  3  FOREIGN KEY (CONTINENTE)
  4  REFERENCES CONTINENTE
  5  DEFERRABLE INITIALLY DEFERRED;

Table altered.

SQL> 
SQL> UPDATE CONTINENTE
  2  SET Nom_CONTINENTE='ifriqiyaahi'
  3  where Nom_CONTINENTE='AFRICA';

0 rows updated.

SQL> 
SQL> UPDATE PAIS
  2  SET CONTINENTE='ifriqiyaahi'
  3  WHERE CONTINENTE='AFRICA';

0 rows updated.

SQL> 
SQL> SELECT Nom_CONTINENTE FROM CONTINENTE;

no rows selected

SQL> SELECT Nom_pais, CONTINENTE FROM PAIS;

no rows selected

SQL> spool off
