SQL> PROMPT PASO 2
PASO 2
SQL> 
SQL> rollback;

Rollback complete.

SQL> 
SQL> ALTER TABLE PAIS
  2  DROP CONSTRAINT PAIS_CONTINENTE_FK;

Table altered.

SQL> 
SQL> ALTER TABLE PAIS
  2  ADD CONSTRAINT PAIS_CONTINENTE_FK
  3  FOREIGN KEY (CONTINENTE)
  4  REFERENCES CONTINENTE ON DELETE SET NULL;

Table altered.

SQL> 
SQL> DELETE CONTINENTE WHERE Nom_CONTINENTE='AFRICA';

0 rows deleted.

SQL> 
SQL> SELECT Nom_CONTINENTE FROM CONTINENTE;

no rows selected

SQL> SELECT Nom_pais, CONTINENTE FROM PAIS;

no rows selected

SQL> spool off
